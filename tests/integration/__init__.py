# Integration tests package
"""
Integration tests for component interactions.

These tests may be slower and can test interactions between components,
but should still avoid external service dependencies where possible.
"""